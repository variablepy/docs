---
title: "Create Task"
description: "Submit an asynchronous captcha-solving task."
---

# Create Task

`POST /createTask`

Creates a new captcha-solving task. Returns a `task_id` for polling.

## Request Body

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `type` | string | Yes | Task type (see below) |
| `task` | object | Yes | Task parameters (see each task type) |
| `key` | string | Yes | Your API key |

Alternatively, `tasktype` can be used instead of `type`, and `apikey`/`clientKey` instead of `key`.

## Response

```json
{ "status": "queued", "task_id": "1722000000000" }
```

## Task Types

| Task Type | Proxy Required | Price / 1k Solves |
| --- | --- | --- |
| `HCaptchaCaptchaToken` | Yes | \$1.50 |
| `HCaptchaEnterpriseToken` | Yes | \$2.00 |
| `TurnstileCaptcha` | Yes | \$0.09 |
| `TurnstileCaptchaProxyless` | No | \$0.09 |
| `CloudflareJsdCookie` | Yes | \$0.20 |
| `AWSWafToken` | Yes | \$0.40 |
| `AWSWafTokenProxyless` | No | \$0.40 |
| `KasadaCtToken` | Yes | \$0.80 |
| `DataDomeTagsCookie` | Yes | \$0.80 |
| `ReCaptchaV3Token` | Yes | \$0.50 |
| `ReCaptchaV3TokenProxyLess` | No | \$0.50 |
| `ReCaptchaV3EnterpriseToken` | Yes | \$0.50 |
| `ReCaptchaV3EnterpriseTokenProxyLess` | No | \$0.50 |
| `FriendlyCaptchaToken` | Yes | \$1.20 |
| `FriendlyCaptchaTokenProxyLess` | No | \$1.20 |
| `TextRecognition` | No | \$0.05 |
| `CaptchaFoxToken` | Yes | \$0.80 |

## Common Task Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `siteurl` | string | URL of the target page |
| `proxy` | string | `http://host:port` or `http://user:pass@host:port` |
| `user_agent` | string | Browser user agent string |
| `sitekey` | string | Captcha site key (hCaptcha, Turnstile, reCAPTCHA) |

## Error Responses

| Status | Code | Meaning |
| --- | --- | --- |
| 400 | `Missing API key` | No key provided |
| 400 | `Missing siteurl` | siteurl is required |
| 400 | `Missing sitekey` | sitekey required for this task type |
| 400 | `Missing proxy` | proxy required for this task type |
| 400 | `Missing user_agent` | user_agent required for hCaptcha tasks |
| 402 | `Insufficient balance` | Not enough balance to solve |
| 403 | `Invalid API key` | Unknown API key |
