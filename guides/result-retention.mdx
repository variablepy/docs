---
title: "Result Retention"
description: "How long HydroSolve stores task results."
---

# Result Retention

Task results are stored for **5 minutes** after solving.

After that, the result is cleaned up and `getResult` will return `solving` (no result found).

## Recommendation

Poll every 1-3 seconds. Most tasks complete well within the 5-minute window.
